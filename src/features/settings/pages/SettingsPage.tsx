import { Languages, Moon, Sun } from "lucide-react";
import { Button } from "@/components/ui/button";
import { useAppStore } from "@/stores/app-store";
export default function SettingsPage() { const { theme, toggleTheme, locale, setLocale } = useAppStore(); return <div className="page"><header className="page-header"><div><span className="eyebrow">APPLICATION</span><h1>Settings</h1><p>จัดการการแสดงผลและภาษาของอุปกรณ์นี้</p></div></header><section className="settings-card"><div><span className="setting-icon">{theme === "light" ? <Sun /> : <Moon />}</span><div><h2>Color theme</h2><p>Current: {theme}</p></div><Button variant="outline" onClick={toggleTheme}>Switch theme</Button></div><div><span className="setting-icon"><Languages /></span><div><h2>Language / ภาษา</h2><p>Current: {locale === "th" ? "ไทย" : "English"}</p></div><Button variant="outline" onClick={() => setLocale(locale === "th" ? "en" : "th")}>{locale === "th" ? "Use English" : "ใช้ภาษาไทย"}</Button></div></section></div>; }
