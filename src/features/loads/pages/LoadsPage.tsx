import { Weight } from "lucide-react";
import { ModulePlaceholder } from "@/components/pages/ModulePlaceholder";
export default function LoadsPage() { return <ModulePlaceholder eyebrow="LOAD DEFINITION" title="Loads" description="กำหนด Load Cases และ Load Combinations" emptyTitle="No loads defined" emptyDescription="Load inputs and unit-safe conversion are not implemented in Phase 1." icon={Weight} />; }
