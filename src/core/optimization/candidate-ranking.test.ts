import{describe,expect,it}from"vitest";import{rankCandidates}from"@/core/optimization/candidate-ranking";const candidates=[{id:"A",label:"A",massKgPerM:50,maximumDeflectionM:.02,maximumUtilization:.8,hasUnevaluatedChecks:false},{id:"B",label:"B",massKgPerM:40,maximumDeflectionM:.04,maximumUtilization:.7,hasUnevaluatedChecks:false},{id:"C",label:"C",massKgPerM:35,maximumDeflectionM:.02,maximumUtilization:null,hasUnevaluatedChecks:false}];describe("candidate ranking",()=>{it("ranks eligible candidates by mass",()=>{expect(rankCandidates(candidates,{maximumDeflectionM:.03,maximumUtilization:1,objective:"minimumMass"}).find(x=>x.id==="A")?.rank).toBe(1);});it("excludes incomplete utilization",()=>{expect(rankCandidates(candidates,{maximumDeflectionM:.03,maximumUtilization:1,objective:"minimumMass"}).find(x=>x.id==="C")?.eligible).toBe(false);});});
