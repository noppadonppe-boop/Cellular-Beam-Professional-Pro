import { describe, expect, it } from "vitest";
import { generateCellularGeometry } from "@/core/cellular";
import { extractCellularCheckActions } from "@/core/cellular/checks";
const geometry = generateCellularGeometry({ beamLengthMm: 12000, parentDepthMm: 400, flangeWidthMm: 200, flangeThicknessMm: 16, webThicknessMm: 10, finishedDepthMm: 600, openingDiameterMm: 400, pitchMm: 600, firstOpeningCenterMm: 700, openingCount: 2, openingEccentricityMm: 0, minimumSolidEndZoneMm: 400, steelDensityKgM3: 7850, weldSizeMm: 6, cuttingPattern: "circular-interlock", weldType: "continuous-fillet" });
const samples = [{ xM: 0, axialN: 0, shearN: 100000, momentNm: 0, deflectionM: 0, rotationRad: 0 }, { xM: 6, axialN: 0, shearN: 0, momentNm: 300000, deflectionM: 0, rotationRad: 0 }, { xM: 12, axialN: 0, shearN: -100000, momentNm: 0, deflectionM: 0, rotationRad: 0 }];
describe("cellular action extraction", () => { it("extracts opening moment and symmetric tee forces", () => { const opening = extractCellularCheckActions(geometry, samples).openings[0]; expect(opening?.momentNm).toBeCloseTo(35000, 8); expect(opening?.topTeeAxialN).toBeCloseTo(opening?.bottomTeeAxialN ?? 0, 8); }); it("extracts web-post action with positive shear flow", () => expect(extractCellularCheckActions(geometry, samples).webPosts[0]?.horizontalShearFlowNPerM).toBeGreaterThan(0)); it("marks results as extracted actions", () => expect(extractCellularCheckActions(geometry, samples).openings[0]?.status).toBe("actionExtracted")); });
