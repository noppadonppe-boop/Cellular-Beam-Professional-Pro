export type ArchStabilityInput = Readonly<{ effectiveLengthM: number; effectiveLengthFactor: number; areaM2: number; inertiaM4: number; elasticModulusPa: number; axialCompressionN: number; bendingMomentNm: number; sectionModulusM3: number }>;
export type ArchStabilityScreen = Readonly<{ radiusOfGyrationM: number; slenderness: number; eulerCriticalLoadN: number; eulerReferenceRatio: number; axialStressPa: number; bendingStressPa: number; combinedElasticStressPa: number; status: "screeningOnly"; limitations: readonly string[]; version: "arch-stability-screening-1.0.0" }>;
export class ArchStabilityError extends Error { public constructor(message:string){super(message);this.name="ArchStabilityError";} }
/** Reference elastic stability quantities only; no code capacity or imperfection reduction is applied. */
export function screenArchStability(input:ArchStabilityInput):ArchStabilityScreen{const values=[input.effectiveLengthM,input.effectiveLengthFactor,input.areaM2,input.inertiaM4,input.elasticModulusPa,input.axialCompressionN,input.bendingMomentNm,input.sectionModulusM3];if(!values.every(Number.isFinite)||input.effectiveLengthM<=0||input.effectiveLengthFactor<=0||input.areaM2<=0||input.inertiaM4<=0||input.elasticModulusPa<=0||input.sectionModulusM3<=0)throw new ArchStabilityError("Invalid arch stability input.");const r=Math.sqrt(input.inertiaM4/input.areaM2);const kl=input.effectiveLengthFactor*input.effectiveLengthM;const ncr=Math.PI**2*input.elasticModulusPa*input.inertiaM4/kl**2;const axial=Math.abs(input.axialCompressionN)/input.areaM2;const bending=Math.abs(input.bendingMomentNm)/input.sectionModulusM3;return{radiusOfGyrationM:r,slenderness:kl/r,eulerCriticalLoadN:ncr,eulerReferenceRatio:Math.abs(input.axialCompressionN)/ncr,axialStressPa:axial,bendingStressPa:bending,combinedElasticStressPa:axial+bending,status:"screeningOnly",limitations:["Euler load is an elastic reference, not a design compression resistance.","Combined stress is elastic superposition and excludes second-order effects, imperfections, residual stress, section classification, and code interaction equations."],version:"arch-stability-screening-1.0.0"};}
